# 이 파일을 .env 로 복사해서 값을 채우세요. .env 는 .gitignore 대상입니다.
#
#   cp .env.example .env
#
# GitHub Actions에서는 이 파일을 쓰지 않습니다. 리포 Settings → Secrets에
# 같은 이름으로 등록하면 실제 환경변수로 주입되고, 그쪽이 .env 보다 우선합니다.

# @BotFather 에서 /newbot 으로 봇 생성 후 발급받은 토큰
TELEGRAM_BOT_TOKEN=

TELEGRAM_CHAT_ID=
