{
  "name": "stock-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "tsx src/main.ts",
    "dry-run": "tsx src/main.ts --dry-run",
    "test": "vitest run",
    "test:send": "tsx scripts/send-test.ts",
    "send:preview": "tsx scripts/send-preview.ts",
    "send:links": "tsx scripts/send-broker-links.ts",
    "typecheck": "tsc --noEmit",
    "gen:brokers": "tsx scripts/gen-brokers.ts",
    "build:pages": "rm -rf dist && mkdir -p dist/state && cp index.html dist/index.html && cp state/brokers.json dist/state/brokers.json"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
